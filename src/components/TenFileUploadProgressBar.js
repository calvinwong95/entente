import { Box } from "@mui/material";
import React, { useEffect } from "react";

const TenFileUploadProgressBar = ({ file }) => {
  useEffect(() => {}, []);
  return <Box>Test</Box>;
};

export default TenFileUploadProgressBar;
